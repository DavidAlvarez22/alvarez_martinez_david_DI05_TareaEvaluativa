import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alvarez_martinez_david_DI04_TaEv01',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
